# 변경 이력

아래 버전은 로컬 개발 이력입니다. 공개 초기 릴리즈는 전체 테스트 후 별도로 승인·배포합니다.

## 0.6.7 — 발 고정, 시작 프레임

적용한 애니메이션이 2프레임에서 시작하던 것과, 서 있는 동작에서 IK 발이 흔들리던 문제를 고쳤습니다. 원인을 좇다가 이동 배율과 접지 보정의 근본 결함도 함께 고쳤습니다.

### 시작 프레임

선두 보정 프레임(CMU의 T포즈 첫 행)을 걷어낸 뒤 키를 원본 프레임 자리에 그대로 두어 결과가 2프레임부터 시작했습니다. 구운 키와 핸들을 걷어낸 만큼 앞으로 옮겨 **원본 첫 프레임(보통 1)에서 시작**합니다. 검증은 원본과 같은 프레임에서 재야 하므로 그 뒤에 옮깁니다. 리포트의 프레임 범위도 옮긴 값입니다.

### 서 있는 발이 흔들리는 문제

크라잉(CMU 80_45)에서 원본 발은 클립 전체에서 0.4~0.9cm 안에 머무는데 캐릭터 발은 FK 1.9~2.3cm, IK 1.1~2.1cm를 움직였습니다. 원인은 두 가지였습니다.

- **이동 배율**: 엉덩이 본 원점→발 거리의 비로 배율을 정했는데, 엉덩이 원점이 다리에 대해 어디 놓이는지(골반 위, 다리 사이)가 리그마다 달라 이 비가 실제 다리 길이 비와 어긋났습니다. 예제 캐릭터는 엉덩이 본이 고관절보다 9cm 아래에 있어 비가 30~38% 작았습니다. 땅을 딛은 발은 고관절을 중심으로 다리가 도는 만큼 몸을 밀어내므로 배율이 다리 길이 비와 다르면 발이 그 차이만큼 미끄러집니다. **허벅지+정강이 마디 길이 비**로 바꿨습니다. ACCAD Walk1 실측: 스탠스 한 번의 FK 발 미끄러짐 8.6~9.8cm → 3.5~5.4cm.
- **발 고정**: 남은 미끄러짐은 다리 기하 차이(골반 폭, 마디 비율)에서 오므로 FK로는 없앨 수 없습니다. 발로 끝나는 IK 체인은 **원본 발이 멈춘 구간(스탠스, 프레임당 다리 길이의 0.4% 미만 이동)마다 FK 발 위치의 구간 평균에 원본 발의 미세한 움직임만 얹어** 목표로 삼습니다. 발이 서는 자리는 FK가 정하고 흔들림은 원본만큼만 남습니다. 스윙은 FK를 따르고 앞뒤 6프레임을 선형으로 잇습니다. 한두 프레임 노이즈로 끊긴 스탠스는 하나로 잇습니다(따로 두면 구간마다 평균이 달라 발이 자리를 옮깁니다).
- 발이 FK와 다른 자리로 가면 뿌리→발 축이 돌아가는데, FK 무릎을 그대로 새 축에 투영하면 폴 평면이 크게 돌아 허벅지가 비틀렸습니다(CMU 94_16, 발 8cm 이동 스탠스에서 48°). **무릎을 같은 스윙으로 함께 돌려** 폴 평면을 세우니 2.9~4.1°로 돌아왔습니다. 폴 각도 실측 보정도 같은 기준으로 잽니다.
- 고정한 다리(허벅지·정강이)는 방향이 원본과 다른 것이 의도이므로 **검증 방향 오차에서 뺍니다**. 리포트의 `발 고정` 줄에 적습니다.
- 실측(발 위치 범위 x/y/z, 왼발): 원본 0.6/0.9/1.0cm · FK 1.9/2.3/1.2cm · **IK 0.7/1.3/1.1cm**(간소화 끔 0.6/0.9/1.0cm). 오른발: 원본 0.4/0.5/0.5cm · FK 2.3/2.1/0.9cm · **IK 0.4/0.7/0.4cm**. 프레임 사이 이동 p95: 원본 0.11/0.06cm · FK 0.18/0.16cm · **IK 0.08/0.04cm**.
- 걷기 실측(원본 발이 멈춘 프레임에서 캐릭터 발의 프레임당 이동 p95, 왼/오른): ACCAD Walk1 FK 0.59/0.59cm → **IK 0.17/0.15cm**, WalkTurnAround180 0.50/0.58 → **0.13/0.18**, RunTurnRight90 0.56/0.75 → **0.23/0.24**, CMU 94_16 0.44/0.41 → **0.11/0.11**(원본 0.10~0.14).

### 접지 보정의 근본 결함

- 첫 프레임에서 엉덩이 본 원점을 레스트 높이에 두었는데, 위와 같은 이유로 리그마다 발 높이가 달라집니다. ACCAD 클립은 발이 10~15cm 바닥 아래로 들어가 프레임별 보정이 엉덩이를 **22cm까지 들었다 내렸다** 했습니다. 프레임별 발 최저점의 **하위 10% 지점이 바닥에 오도록 상수만큼 먼저 옮기고**(`바닥 맞춤`, 리포트에 적음) 프레임별 보정은 남은 관통에만 씁니다. 중앙값으로 맞추면 절반의 프레임이 몇 mm씩 파고들어 그 보정이 매 프레임 튑니다.
- IK로 발을 고정한 경우 관통을 몸을 들어 피하지 않고 **스윙 중인 발 목표 자체를 바닥 위로 올립니다**. 몸을 들면 땅을 딛은 반대쪽 다리가 목표에 닿지 못해 발이 엉덩이를 따라 오르내립니다(Walk1 스탠스에서 2~6cm). 스탠스 프레임은 올리지 않습니다 — 거의 펴진 다리의 발목을 3mm만 올려도 무릎이 12° 튀어나옵니다. 남는 관통은 몇 mm입니다(80_45 0.46cm).
- 남은 바닥 관통 보고는 발·발끝 본만 봅니다. 손이나 IK 컨트롤·폴, 소켓 본이 바닥 아래여도 관통이 아닙니다(예제 캐릭터는 팔이 길어 손이 바닥보다 낮게 내려옵니다).
- ACCAD Walk1: 접지 보정량 22.0cm/459프레임 → **0**, 방향 오차 FK 1.23° 유지.

### 회귀 검사

- "간소화 끈 IK는 FK와 일치" 검사는 발 고정을 끄고 잽니다. 발 고정은 발을 FK와 다른 자리에 붙잡는 것이 목적이기 때문입니다.
- 전체 스위트 7개 검사 통과.

시도했다가 되돌린 것: 원본의 엉덩이→발 상대 벡터를 그대로 목표로 쓰는 방식(레스트 기하 차이로 발이 서는 자리가 달라짐), FK와 원본의 차이를 클립 평균 상수로 빼는 방식(걷기에서 스윙 위상마다 차이가 달라 다리 방향이 20~36° 벗어남), 고정한 발도 프레임별 접지 보정에 따라 오르내리게 두는 방식(스탠스에서 2~6cm 상하 이동), 스탠스 프레임까지 발 목표를 바닥 위로 올리는 방식(펴진 다리 무릎이 14° 튐).

## 0.6.6 — IK 다리 뒤집힘

크라잉(CMU 80_45)처럼 다리를 거의 펴고 서 있는 모션을 IK로 적용하면 왼쪽 다리가 제 축 둘레로 180° 돌아간 채 굽히는 문제를 고쳤습니다. 폴 본은 무릎 쪽(정상 위치)에 있는데 허벅지와 발이 뒤집혀 보이는 증상입니다.

### 원인

Blender의 폴 타깃은 무릎 위치를 맞추는 것이 아니라 **루트 본(허벅지)의 X축이 폴을 향하도록** 체인을 돌리고, 무릎이 어느 쪽으로 굽는지는 솔버가 레스트 굽힘 방향으로 정합니다. 지금까지는 FK로 계산한 무릎 위치(뿌리·무릎·발 삼각형)에서 폴 위치를 정했는데, 크라잉의 다리는 굽힘이 0.9°(무릎 측면 오프셋 0.008 / 다리 길이 0.52)로 거의 펴져 있고 허벅지 기준으로는 약 4° **뒤로 꺾여(과신전)** 들어옵니다. 무릎이 레스트와 반대쪽에 있으니 솔버는 허벅지를 180° 돌려서 무릎을 그 자리에 맞췄습니다. 실측에서 IK 허벅지 비틀림이 FK 대비 전 구간 180°였고, 간소화를 꺼도 같았습니다. 이전의 "직전 평면 이어 쓰기"는 굽힘 1.1° 아래에서만 작동해 이 경우를 잡지 못했습니다.

### 수정

- **FK 허벅지의 비틀림에서 무릎이 굽을 방향을 예측**합니다. 레스트에서 무릎이 굽는 방향을 루트 본 로컬로 옮겨 두고, 프레임마다 FK 루트 본 회전을 곱합니다.
- 실측 무릎 방향의 예측 쪽 성분이 루트 본 길이의 5%(약 2.9°)에 못 미치면 부족한 만큼 예측 방향을 더합니다. 예측 성분이 항상 문턱 이상이 되므로 **어떤 경우에도 반대쪽으로 뒤집히지 않고**, 문턱에서 더하는 양이 0이라 평면이 홱 바뀌지도 않습니다. 충분히 굽은 관절은 이전과 완전히 같게 실측 무릎 방향을 따릅니다.
- 이전 프레임의 평면을 들고 다니던 방식은 없앴습니다. 예측이 프레임마다 FK에서 나오므로 잘못된 평면이 구간 내내 굳어질 여지가 없습니다.
- 폴 각도 실측 보정은 예측이 개입한 프레임을 표본에서 뺍니다. 그 프레임의 무릎은 원래 그 자리에 있을 이유가 없어 상수 잔차로 세면 안 됩니다.
- 여섯 모션 실측(IK 허벅지 비틀림이 FK와 어긋난 최대각, 왼/오른 다리): CMU 80_45 **180.0°/1.6° → 3.7°/2.3°**, 94_16 2.5°/**28.5° → 2.4°**, 14_17 2.6°/2.2° (변화 없음), ACCAD Walk1 33.7°/21.4° → 23.3°/20.5°, RunTurnRight90 20.4°/20.6° → 17.9°/17.6°, Crouch 17.0°/26.0° → 17.0°/26.3°. 팔은 폴 보정값까지 여섯 모션 모두 이전과 동일합니다.
- 대가는 펴진 다리에서 무릎이 예측 쪽으로 옮겨 가는 중간 관절 오차입니다. 80_45에서 0.0195 → 0.0298(다리 길이 0.52 대비 약 2%). 다른 다섯 모션은 ±0.0002 안입니다. 키는 −3%~+1%로 사실상 같습니다.
- 회귀 검사의 "간소화 끈 IK는 FK와 0.5° 안" 기준을 2.0°로 조정했습니다. 합성 걷기의 다리·오른팔은 굽힘이 정확히 0이고 왼팔은 최소 1.7°여서, 그 구간의 무릎 방향이 허벅지 비틀림을 따르는 새 동작과 정확 재현은 양립하지 않습니다(실측 1.19°). 그 외 검사는 모두 그대로 통과합니다.

시도했다가 되돌린 것: 문턱을 굽힘 크기 자체(예측 성분이 아닌 `|side|`)로 재는 방식은 과신전이 문턱의 절반을 넘으면 다시 뒤집히고, 굽은 관절에서도 예측을 항상 더하는 방식은 팔꿈치 위치와 팔 폴 보정값(−6° → −15~−21°)을 흔들었습니다. 문턱 0.1은 합성 걷기 방향 오차 2.4°, 0.05는 1.2°였습니다.

## 0.6.5 — 손목 꺾임, IK 다리 튐

손이 90°쯤 꺾여 들어오는 문제와 IK에서 다리가 한 번씩 튀는 문제를 고쳤습니다.

### 모션 파일에 관절 길이가 없는 경우

BVH는 관절의 위치만 담고 방향은 자식 관절 위치로 유추합니다. 손은 그 정보가 없어 Blender가 방향을 지어내는데(항상 수직), 거기에 캐릭터 손을 맞추느라 손목이 통째로 꺾였습니다. CMU 94_16 실측에서 모션 원본 손목은 0.8~28.8°인데 캐릭터 손목은 144.7~176.8°였습니다.

- 두 가지 형태를 모두 판별합니다. 자식이 모두 제 머리에 겹친 관절(CMU의 `Hips`·`Spine1`·`LeftHand`·`RightHand`)과, End Site 없이 끝나는 말단(Bandai Namco의 `Hand_L`·`Head`·`Toes_L`)입니다. 둘 다 Blender가 같은 기본 길이를 넣으므로 리그에서 가장 긴 본과 견주어 걸러냅니다. 실측에서 지어낸 본은 0.02~1.29%, 진짜 짧은 본(발끝)은 11.9% 위였습니다.
- 그런 본은 지어낸 방향에 맞추는 대신 **부모 부위가 쓴 회전을 물려받아** 부모를 따라가게 합니다. 손목 자체의 회전은 프레임별 모션에 들어 있으므로 잃지 않습니다.
- 맞추지 않는 방향은 검증 오차에서도 뺍니다. 그대로 세면 100°가 넘는 값이 찍혀 정작 봐야 할 본의 오차가 묻힙니다. 적용 리포트에 `모션 본 방향 없음` 줄로 남깁니다.
- 세 출처 실측(손목 굽힘이 원본과 어긋난 각): CMU 165.1° → **0.6°**, 166.3° → **0.8°**. Bandai Namco 149.7° → **0.6°**, 157.3° → **0.4°**. ACCAD는 지어낸 본이 없어 13.6°·14.1° 그대로입니다. 여섯 모션 모두 검증 방향 오차는 전후 동일했습니다.

### IK에서 다리가 튀는 문제

곡선 간소화를 끄면 튐이 사라져(7.5° → 0.01°), 간소화가 닫힌 운동 사슬을 깨는 것이 원인임을 확인했습니다. 척추·엉덩이 곡선과 IK 목표 곡선을 따로 솎아내면 다리 뿌리와 발 사이 거리가 흔들리는데(실측 0.0095 / 다리 길이 0.5192), 다리가 거의 펴진 구간에서는 이 거리가 1%만 달라져도 무릎 각이 6.5° 돕니다. 엉덩이가 위아래로 움직일 때가 바로 다리가 펴지는 구간입니다.

- **두 단계로 굽습니다.** FK 곡선을 먼저 굽고, 그 곡선이 실제로 만든 자세를 읽어 그 위에서 IK 목표를 잡습니다. 다리 삼각형이 닫힙니다.
- IK 컨트롤과 엉덩이 이동 곡선은 회전 곡선보다 **4배 조인 허용치**로 굽습니다. 무릎이 목표 오차를 증폭하기 때문입니다.
- 무릎이 거의 펴져 폴 평면이 정의되지 않는 구간에서는 직전에 안정적이던 평면을 이어 씁니다.
- 셋을 함께 넣어야 잡혔습니다. 하나씩은 효과가 없거나 오히려 나빠졌습니다.
- 여섯 모션 실측(FK 대비 초과 급변 / 2°를 넘는 프레임 수): CMU 01_13 13.03°/121 → **4.39°/14**, 79_22 2.47°/3 → **1.23°/0**, 127_21 2.97°/2 → **1.96°/0**, ACCAD WalkTrunChange 7.47°/77 → **3.95°/9**, RunTurnRight90 4.93°/10 → **1.70°/0**, Bandai guide_old_001 1.02°/0 → **0.97°/0**.
- 대가는 키 +7~26%입니다. 접지 미끄러짐은 늘지 않았고(대체로 개선), **검증 방향 오차는 여섯 모션 모두 좋아졌습니다**(최대 5.22° → 1.24°). CMU 76_01에서 IK와 FK의 방향 오차가 1.76°로 같아져, 예전의 "IK가 FK의 약 2배" 특성이 없어졌습니다.

시도했다가 되돌린 것: 접지 보정량 다듬기(ACCAD에서 7.47° → 9.17°로 악화), 폴 곡선 추가 조이기(키 +45%에 이득 없음), 체인 길이 기준 컨트롤 허용치(CMU 01_13에서 5.20° → 9.34°로 악화).

### 고치지 못한 것

2본 IK는 루트·끝점·폴로 해가 완전히 결정되므로 위팔이 제 축 둘레로 도는 비틀림을 재현할 수 없습니다. 그 회전은 팔꿈치 위치를 바꾸지 않아 IK가 볼 수 없는 자유도입니다. 실측에서 방향(스윙) 오차는 2.5°인데 축 둘레 비틀림은 72°까지 벌어집니다. 손·발의 세계 방향은 끝본 보정으로 맞추므로 손 모양은 맞고, 팔뚝 메시만 비틀립니다. 예제 리그에 트위스트 본이 없어 흡수할 곳도 없습니다. README의 **IK 전환**에 한계로 적었습니다.

## 0.6.4 — 굽기 키 24% 추가 감소

곡선 간소화가 매듭을 놓는 방식을 고쳐 같은 허용 오차에서 키를 더 줄였습니다. 허용치 판정은 그대로라 오차 보장이 바뀌지 않습니다.

- 지금까지는 구간 하나로 적합해 보고 **가장 어긋난 지점에서 쪼개는** 하향식이라 매듭이 최적 위치에 놓이지 않았습니다. 쪼갠 뒤 **이웃 구간을 다시 합쳐 보는 패스**를 더 못 합칠 때까지 돌립니다. 합치기 판정은 기존 허용치 검사를 그대로 쓰므로 통과한 구간만 합쳐집니다.
- 세 출처 11개 모션(CMU 4, Bandai Namco 3, ACCAD 4 · 119~1,693프레임)에서 **19.0~28.6%, 합계 24.0%** 줄었습니다. 곡선 회전 오차는 모든 모션에서 허용치 1.0° 안(최대 1.0001°)에 그대로 남았습니다.
- 검증 방향 오차는 매듭 위치가 달라지면서 모션마다 −1.90°~+0.98° 범위로 흔들리고 평균 −0.04°로 치우침이 없습니다. 가장 크게 변한 모션은 오히려 좋아진 쪽(6.64° → 4.74°)이었습니다.
- CMU 76_01 · 465프레임 · FK 실측: 기본 허용 오차 1.0°에서 4,343 → **3,303키**, 방향 오차 1.84° → 1.79°. 굽기 시간은 CMU 94_16(1,016프레임)에서 2.1초 → 2.4초입니다.
- README의 간소화 실측 표를 다시 재어 갱신했습니다. `발 침하`로 적혀 있던 칸은 실제로 재는 값인 `엉덩이 이동 오차`로 이름을 바로잡았습니다.

말단 본에 허용치를 더 주는 방식(변위 기준 가중)도 재 봤지만, 중간 본이 느슨해지면서 오차가 체인을 타고 누적돼 키 6% 감소에 검증 오차 6.39° → 9.11°를 치렀습니다. 교환비가 나빠 넣지 않았습니다.

## 0.6.3 — 모션 정면 정렬

모캡 라이브러리는 배우가 캡처 공간에서 향한 방향이 클립마다 달라, 본 방향을 그대로 옮기면 캐릭터가 정면에서 90°쯤 돌아간 채 생성되는 모션이 섞여 있었습니다. 실측으로 CMU `94_16`은 첫 프레임에서 −88.0° 돌아가 있었고, 모션 원본 리그의 `Hips` 방향과 결과가 완전히 일치해 리타게팅 계산이 아니라 원본 데이터의 캡처 방향이 원인임을 확인했습니다.

- `⚙ 상세 설정`에 **정면 정렬**을 넣었습니다(기본 켜짐). 첫 프레임(선두 보정 프레임을 걷어낸 뒤)의 엉덩이 정면을 캐릭터 레스트 정면에 맞춥니다.
- 세계 수직축 둘레의 **방위각만** 되돌립니다. 기울기를 건드리면 눕거나 기운 동작이 깨지므로 피치·롤은 그대로 둡니다. 엉덩이 본이 거의 수직이어도 수평으로 누운 축을 골라 재고, 기준 축을 못 찾으면 보정하지 않습니다.
- 회전과 함께 **엉덩이 이동 궤적에도 같은 회전을 적용**하므로 전진 방향이 몸 방향과 어긋나지 않습니다. 검증 오차 계산에도 같은 보정을 넣어 정렬이 충실도 수치를 흔들지 않습니다(CMU `94_16` 실측: 켜짐·꺼짐 모두 2.606°).
- 정렬한 각도를 적용 리포트에 적습니다. 보정이 필요 없던 모션과 정렬을 끈 경우도 구분해 표시합니다.
- CMU `94_16` 실측: 정렬 끔 → 첫 프레임 −88.0°, 정렬 켬 → **0.0°**(이후 프레임의 18°는 모션 자체의 회전).

## 0.6.2 — 모션 폴더 열기, 보관 위치 안내, 폴더 통째 가져오기

받아 둔 모션이 어디에 쌓이는지 애드온 안에서 바로 보고, Blender를 새 버전으로 올렸을 때 이전 폴더의 모션을 한 번에 옮겨 올 수 있게 했습니다.

- 사이드바 맨 아래와 `⚙ 상세 설정`에 **모션 폴더** 버튼을 넣었습니다. 팝업이 현재 폴더 경로와 보관 중인 BVH 개수를 보여 주고, **폴더 열기**로 운영체제 파일 탐색기를 엽니다. 기본 위치가 아닌 폴더를 쓰고 있으면 **기본 위치로** 버튼이 함께 나옵니다.
- 팝업에 보관 규칙을 적었습니다. 받은 파일은 애드온 설치 폴더가 아니라 Blender 사용자 데이터 폴더에 남으므로 **애드온을 업데이트하거나 지웠다 다시 깔아도 그대로**입니다. 폴더가 새로 생기는 경우는 Blender 자체를 새 버전으로 올렸을 때뿐입니다.
- **모션 가져오기**를 추가했습니다. 탐색창에서 이전 Blender 버전의 모션 폴더를 고르면 하위 폴더 구조 그대로 BVH를 복사하고 `motions.json`의 이름·분류·태그까지 합칩니다. 같은 이름의 기존 파일은 덮어쓰지 않고, 현재 모션 폴더 자신이나 그 상위 폴더는 가져오기를 막습니다. 복사는 임시 파일에 쓴 뒤 교체하므로 중간 상태가 남지 않습니다.
- 이전 Blender 버전 폴더에 모션이 남아 있으면 팝업이 그 경로를 찾아 **여기서 가져오기**·**이 폴더 사용**·**열기** 버튼을 함께 띄웁니다. 탐색창도 그 폴더에서 열립니다.
- 인덱스 기록을 `merge_manifest`로 묶어 폴더를 통째로 가져올 때 `motions.json`을 한 번만 씁니다. 기존 `update_manifest`는 이 경로를 그대로 씁니다.
- Windows CI에서 검사 실패 로그를 찍다가 콘솔 코드페이지(cp1252) 인코딩 오류로 멈춰 진짜 실패 원인이 묻히던 문제를 고쳤습니다. UI 계약 검사도 경로 구분자가 역슬래시인 환경을 함께 받아들입니다. `v0.6.1` 태그는 이 문제로 Release 자산을 게시하지 못했고, 같은 내용을 `v0.6.2`로 배포합니다.

## 0.6.0 — 공개 모션 5,908종, 사이드바 목록, 이름·분류 직접 정리

세 곳의 공개 출처를 합쳐 카탈로그를 485종에서 5,908종으로 넓혔고, 목록을 사이드바로 되돌려 검색·분류·이름 정리를 한 화면에서 하게 했습니다. 라이선스가 다른 출처가 섞이지 않도록 비상업 데이터를 기본으로 숨깁니다. v1 절차 동작 생성은 제거했습니다.

### ACCAD 298종 추가, ZIP 배포본 지원

- 세 번째 출처로 ACCAD Open Motion Project를 넣어 카탈로그가 **5,908종**이 되었습니다. **CC BY 3.0**이라 출처를 표기하면 상업 제품에도 쓸 수 있어, 상업 사용 가능 분량이 2,533 → **2,831종**으로 늘었습니다.
- ACCAD는 개별 파일 주소가 없어 ZIP으로만 배포됩니다. 다운로더에 압축 배포본 경로를 넣었습니다. 압축 파일의 SHA-256을 먼저 검증하고, 안의 BVH를 모두 풀어 같은 묶음의 항목을 한 번에 등록합니다(`Female1` 81개를 한 번 받아 전부 인덱싱). 두 번째 항목부터는 네트워크를 쓰지 않습니다.
- 압축 해제는 멤버마다 절대 경로·`..`·BVH 아닌 확장자를 걸러내고, 풀었을 때 합계가 256MB를 넘으면 중단하며, 카탈로그가 아는 멤버는 크기와 SHA-256을 대조합니다. 검증 실패 시 파일과 인덱스를 남기지 않습니다. 허용 호스트에 `accad.osu.edu`를 더했습니다(총 2개).
- 실제 다운로드로 끝까지 확인했습니다: `Female1_bvh.zip`(5.2MB) 검증 → BVH 81개 해제 → `motions.json` 81건 등록 → 21/22 부위로 적용.

### Bandai Namco Research 3,077종 추가, 라이선스 구분

- 두 번째 출처를 넣어 카탈로그가 **5,610종 / 50분류**가 되었습니다. Bandai Namco Research Motion Dataset의 BVH 3,077개는 이미 `raw.githubusercontent.com`에 있어 허용 호스트를 늘리지 않았습니다. 파일명이 `dataset-N_{동작}_{스타일}_{번호}` 구조라 별도 인덱스 없이 동작 25종과 감정·연령 스타일 19종을 그대로 이름·분류·태그로 옮겼습니다.
- 이 데이터는 **CC BY-NC 4.0(비상업 전용)**입니다. `MotionAsset.commercial_use`를 추가하고, 기본 목록에서 비상업 출처를 뺐습니다. `⚙ 상세 설정`의 **비상업 데이터 포함**을 켜면 들어오고 목록에 `NC` 배지가 붙습니다. 적용하면 상태 줄과 리포트에 상업 사용 금지와 이용 조건을 남깁니다. 라이선스 조건은 검색 전에 걸러 목록에 안 들어갈 항목의 검색 문자열을 만들지 않습니다.
- 리타게팅에 **Unity 휴머노이드 계열** 규격을 추가했습니다. Rigify의 hips 본 이름이 `spine`이라, 이 규격이 없으면 `UpperArm_L`·`UpperLeg_L` 계열 리그의 `Spine`이 hips로 잘못 잡히고 다리 계층 검증이 깨져 매핑이 16/22로 떨어졌습니다. 규격 추가로 **21/22(95%)**가 되었고, 남은 1개는 원본에 척추가 두 단뿐이라 실제로 없는 부위입니다. 회귀 검사는 리그 8종으로 늘었습니다.
- 카탈로그 검색 문자열을 아직 받지 않은 항목에 한해 캐시합니다. 목록 갱신은 기본(상업만 2,533종) 33ms, 전체 5,610종 51ms이며 비용은 전부 Blender 컬렉션 재구축입니다(`browse` 자체는 1ms 미만).

### 미분류 포함 2,533종, 이름·분류 직접 편집

- 설명이 없거나 `Unknown`·`clean`뿐이라 제외했던 CMU 309종을 `미분류` 분류로 넣었습니다. 카탈로그가 **2,533종 / 49분류**가 되어, 크기창을 벗어난 10종을 빼면 저장소의 BVH를 전량 담습니다.
- **✎** 버튼이 이름과 함께 **분류**도 바꿉니다(`catani.motion_rename`). 둘 다 모션 폴더의 `motions.json`에 저장되므로 미분류 항목을 직접 정리해 쓸 수 있습니다. 카테고리 필터가 켜진 상태로 분류를 바꾸면 필터가 항목을 따라 옮겨가 목록에서 사라지지 않습니다.
- `motion_header`가 병렬 조회 중 한 번 실패하면 바로 포기하던 문제를 고쳤습니다(길이를 늘려 재시도). 이 때문에 프레임 수가 0이던 1종을 채웠습니다.

### 카탈로그 485 → 2,224종

- 상한이 소스가 아니라 스크립트의 `PER_CATEGORY = 14`였습니다. CMU 미러에는 BVH가 2,548개 있고, 크기·설명 조건을 통과하는 2,229개 중 **485개만** 카탈로그에 들어가고 있었습니다.
- 버려지던 852개를 분류로 흡수했습니다. 동물 흉내(`(human subject)` 꼬리표로 판정), 2인 상호작용, 행진, 검술, 요가·체조, 공구 작업, 그네, 집안일, 감정 표현, 절뚝거림, 손 뻗기·놓기, 장면 연기를 새로 넣어 **48개 분류**가 되었습니다. 그래도 안 걸리는 설명은 `기타`로 받아 하나도 버리지 않습니다.
- 설명 정규화를 넣었습니다. `RunningWideLeft   CleanedGRS`처럼 도구 꼬리표가 붙고 낱말이 붙어 있던 항목을 `Running Wide Left`로 다듬어 분류와 표시가 모두 맞습니다. 의미 없는 `clean` 항목 98개는 제외합니다.
- 크기창을 60KB~12MB로 넓히고 캡을 풀어 **2,224종**이 되었습니다. 카탈로그 JSON은 1.1MB, 원본 합계 2.9GB이며 실제로 받는 것은 고른 파일뿐입니다.
- 항목마다 필요한 Range 요청을 8개씩 병렬로 던져, 2천 건이 넘는 카탈로그도 한 번에 만들 수 있습니다.
- 카탈로그 MotionAsset 목록을 한 번만 만들어 두고, 이미 받은 파일 검사도 폴더 단위로 바꿨습니다. 항목이 4.6배 늘었는데 검색 갱신은 36ms → **20.5ms**로 오히려 빨라졌습니다(485종 시절 8.7ms).

### 목록 이름 바꾸기와 카탈로그 이름 복원

- 받아 둔 파일이 목록에서 `105 59`처럼 숫자로만 보였습니다. `motions.json`에 항목이 없으면 파일 이름만 남았기 때문입니다. 이제 폴더 안 상대 경로를 공개 카탈로그의 `local_path`와 맞춰(`CMU/105_59.bvh`) 이름·태그·출처·체크섬을 그대로 살립니다. 예전 버전이 받아 둔 파일과 손으로 옮긴 파일이 함께 복구됩니다.
- 이름 옆 **✎** 버튼(`catani.motion_rename`)으로 목록 이름을 직접 바꿉니다. 값은 모션 폴더의 `motions.json`에 적히므로 다시 읽어도 유지되고, 이름만 바꿔도 출처와 체크섬은 카탈로그에서 이어받습니다. 아직 받지 않은 모션은 적어 둘 파일이 없어 버튼이 잠깁니다.
- 이름을 바꿀 수 있는 폴더를 설정한 모션 폴더 하나로만 판단해, 함께 훑는 폴더(`bundled_library_path`)에 있는 파일이 모두 "동봉 예제"로 거부되던 문제를 고쳤습니다. 이제 파일이 실제로 들어 있는 폴더에 적고, 쓸 수 없는 위치일 때만 막습니다.
- 검색이 파일 이름도 훑습니다. 이름을 손으로 바꾼 뒤에도 `141_19` 같은 원본 번호로 찾을 수 있고, 검색 중에 이름을 바꿔도 항목이 목록에서 사라지지 않습니다.
- 매니페스트 갱신을 `motion_library.update_manifest` 하나로 모았습니다. 다운로더도 이 함수를 쓰며, 이미 손으로 바꿔 둔 이름은 다시 받아도 덮어쓰지 않습니다.

### 사이드바 목록 복귀, 표본 포즈 그림 제거

- 팝업이 오히려 불편했습니다. `모션 샘플 보기` 팝업을 없애고 검색·카테고리·`받은 모션만`·목록을 사이드바로 되돌렸습니다. 목록은 기본 18줄이고 손잡이로 더 늘릴 수 있습니다.
- 정지 그림으로는 동작을 판단할 수 없어 **표본 포즈 미리보기를 제거**했습니다. `motion_preview.py`(271줄), 아이콘 프리뷰 캐시, `test_motion_preview.py`가 함께 빠졌습니다. 동작 확인은 `뷰포트에서 재생` 하나로 모읍니다.
- 목록 아래 정보는 형식·크기·카테고리와 프레임 수·길이 세 줄로 줄였습니다. 길이는 BVH의 `Frames`/`Frame Time` 두 줄만 읽는 `motion_library.read_length`로 얻고, 패널이 자주 다시 그려지므로 mtime 기준으로 캐시합니다. 태그와 설명은 `ⓘ 출처` 팝업에 있습니다.

### 절차 동작 생성 제거

- `보조 · 절차 동작 비교` 패널을 없앴습니다. 0.3.0에서 모션 라이브러리로 전환한 뒤에도 v1 실험 코드가 남아 있어, Player v1 샘플 리그에서만 동작하는 기능이 사이드바에 계속 보였습니다.
- `core.py`(절차 곡선 샘플러)와 `engine.py`(세션 미리보기·확정) 338줄, 연산자 4개(`inspect_rig`·`preview`·`confirm`·`cancel`), 프로퍼티 5개, `save_pre`/`undo_pre`/`redo_pre` 핸들러를 함께 제거했습니다. 남은 미리보기는 모션 라이브러리 경로 하나입니다.
- 재로드 검사용 예제는 `blender_retarget.py`가 굽기 결과를 `artifacts/CatAni_Motion_Demo.blend`로 저장하도록 옮겼습니다. 절차 동작에 묶여 있던 `blender_smoke.py`·`blender_curves.py`·`blender_operators.py`는 지우고, 등록 해제·재등록 확인은 남은 검사에 넣었습니다.
- README의 Graph Editor 편집 안내를 실제 굽기 결과인 `rotation_quaternion` 곡선 기준으로 고쳤습니다. 이전 안내는 절차 동작만 쓰던 축-각도 채널을 설명하고 있었습니다.

### 모션 샘플 팝업과 표본 포즈 미리보기

- 좁은 `N` 사이드바에서 485종을 고르기 어려웠습니다. 사이드바는 **모션 샘플 보기** 버튼·대상·적용만 남기고, 검색·목록·상세·미리보기·적용을 폭 1080의 **팝업**으로 옮겼습니다. 목록은 16줄로 늘어났고 확인 버튼이 곧 `적용하기`입니다.
- **표본 포즈 미리보기**를 넣었습니다. BVH 헤더와 표본 프레임 6줄만 읽어 순운동학으로 풀고 3/4 시점 스틱 피겨로 그립니다(직전 표본은 흐린 잔상). CMU 1,918프레임 파일에서 파싱 8ms·그리기 22ms로, 파일을 씬에 불러오지 않고도 동작을 알아볼 수 있습니다. 앞머리 8%는 T포즈 보정 구간이라 건너뜁니다. 뼈대 오프셋이 가장 긴 축을 위로 잡아 Z-up BVH도 바로 섭니다.
- **뷰포트에서 재생**은 모션 원본 리그를 임시 컬렉션으로 불러와 재생 구간을 잡고 재생합니다. `미리보기 정리`는 임시 리그와 프레임 범위를 되돌리고, 그대로 `적용`하면 같은 리그를 원본으로 넘겨 파일을 다시 읽지 않습니다. 아직 받지 않은 모션은 내려받은 뒤 자동으로 재생까지 이어집니다.
- 팝업에 **카테고리** 필터와 **받은 모션만** 토글을 넣었습니다. 받아 둔 파일도 출처 주소를 통해 카탈로그 카테고리를 이어받습니다.

### 키 91% 감소와 IK 전환

- 곡선 간소화를 **3차 베지어 최소제곱 적합**으로 바꿨습니다. 구간마다 접선 길이를 데이터에 맞춰 풀기 때문에, 키를 표본 프레임에만 놓고 핸들을 자동에 맡기던 방식보다 같은 오차에서 매듭이 62~66% 적게 남습니다. 실제 CMU 걷기에서 키가 42,319 → 3,897개(**91% 감소**)입니다. 핸들 x를 구간의 1/3 지점에 고정해 x(t)를 선형으로 만들었으므로 적합 계산과 Blender의 평가가 정확히 일치합니다.
- 기본 허용 오차를 0.5°에서 **1.0°**로 올렸습니다. 실측에서 1.0°는 키 90% 감소·방향 오차 2.16°, 0.5°는 83%·1.12°입니다.
- **선두 보정 프레임을 걷어냅니다.** CMU/cgspeed BVH는 첫 행의 회전 채널이 모두 0인 T포즈 보정 프레임으로 시작합니다(76_01은 회전 절대합이 1행 106, 2행 1111, 2→3행 변화는 1.6°). 지금까지 적용한 모든 CMU 모션에 1프레임짜리 T포즈 팝이 들어 있었습니다. 검증 지표가 "원본과 같은가"만 보기 때문에 드러나지 않았습니다. 방향 변화가 평소의 8배를 넘는 선두 프레임만 제거합니다.
- **IK 전환**을 넣었습니다(기본 켜짐). 팔다리를 IK 컨트롤 본으로 굽고, 체인 길이·컨트롤 본·폴 타깃을 **리그의 IK 컨스트레인트에서 직접 읽습니다.** 본 이름 규칙이나 리그 규격 표가 필요하지 않고, 컨트롤 본이 체인 아래에 있어 순환이 되는 구성은 건너뜁니다. IK가 없는 리그에서는 조용히 FK로 돌아가며 그 사실을 리포트에 적습니다.
- 폴 타깃은 `pole_angle` 규약을 추측하지 않습니다. **레스트 자세에서 폴이 체인 삼각형에 대해 갖던 관계를 현재 삼각형으로 옮깁니다.** 리그의 레스트 자세가 그 리그의 IK 해와 어긋난 경우를 위해, 구운 뒤 Blender가 실제로 푼 무릎 위치를 읽어 상수 잔차를 재고 되돌립니다(예제 리그에서 팔 −6.01°, 다리 −2.06°, 프레임 간 분산 0).
- 2본 IK는 팔뚝·정강이의 비틀림을 폴이 정하므로 손·발이 축을 따라 돌아갈 수 있습니다. Blender가 푼 결과를 읽어 그 위에서 끝본의 세계 방향을 FK와 같게 맞춥니다.
- 간소화를 끈 IK 결과는 FK와 목표 위치 오차 0.0001, 중간 관절 오차 0.0000, 방향 오차 0.020°로 일치합니다. 같은 허용 오차에서 IK 경로의 방향 오차는 FK의 약 2배인데(4.21° 대 2.16°), 척추 곡선의 오차가 IK에서는 굽힘 각도를 바꾸는 레버로 작용하기 때문입니다. 상류 본만 더 조이는 방안은 키가 36% 늘고 오차는 3.26°까지만 줄어 채택하지 않았습니다.
- `노이즈 완화` 옵션을 넣었으나 **기본은 끔**입니다. 실측에서 완화 3프레임 + 0.5°(4,829키 · 5.75°)가 완화 없이 1.0°(4,339키 · 2.16°)보다 키도 많고 오차도 컸습니다. 허용치 조절이 항상 우월합니다.
- 접지 보정을 완화·간소화 이후의 최종 포즈로 다시 풀도록 `_sample`을 3단계로 나눴습니다.

### 편집 가능한 베지어 곡선

- 구운 결과가 **프레임마다 선형 키**가 박힌 곡선이라 손으로 고치기 어려웠습니다. 이제 `곡선 간소화 오차`(기본 0.5°) 안에서 키를 솎아내고 **오토 클램프 베지어** 핸들로 만듭니다. 실제 CMU 걷기(76_01)에서 키 42,410개 → 19,063개(55% 감소), 모든 프레임 방향 오차 1.28°, 발 바닥 침하 0.003입니다. 0으로 두면 예전처럼 모든 프레임에 키를 남깁니다.
- 솎아내기는 Ramer-Douglas-Peucker 변형으로 **한 부위의 쿼터니언 4채널을 묶어** 판정합니다. 채널마다 키 위치가 달라지면 중간 프레임에서 회전이 뒤틀리기 때문입니다.
- 선형 보간을 기준으로 솎아낸 뒤, 만들어진 **베지어 곡선을 직접 평가해** 허용치를 넘는 프레임을 키로 되살립니다. 허용 오차는 요청값이 아니라 실측값으로 보장되며 리포트에 실측 회전·이동 오차를 함께 적습니다.
- 오버슈트가 발을 바닥 아래로 밀지 않도록 핸들을 **오토 클램프**로 고정합니다.
- 쿼터니언 부호 연속성 보정을 넣었습니다. `to_quaternion()`은 인접 프레임에서 부호가 뒤집힐 수 있고, 그 상태로 키를 솎아내면 해당 구간이 통째로 반대로 돌아갑니다. 매 프레임 선형 키에서는 한 프레임 튐으로 묻혀 있던 결함입니다.
- 간소화를 켜면 보간 구간이 곡선 전체로 퍼지므로 **검증 프레임을 7개에서 13개로** 늘려 잽니다.
- 상태 줄에 키 감소율을, 리포트에 허용 오차·감소율·실측 오차를 적습니다.
- 개발 실행기와 검사·패키징 스크립트가 **Blender 실행 파일을 스스로 찾습니다**. `BLENDER_BINARY`가 비어 있으면 PATH와 흔한 설치 위치(Windows는 `Program Files\Blender Foundation`·`%LOCALAPPDATA%\Programs`·Steam·각 고정 드라이브의 상단과 `Tools`·`Apps`·`Programs`·`Program Files`, macOS는 `/Applications`·`~/Applications`·`/opt`·Homebrew)를 훑습니다. 폴더 이름이 아니라 `--version` 출력으로 실제 버전을 읽고 매니페스트 `blender_version_min`을 만족하는 가장 높은 버전을 고르므로, 구버전이 함께 깔린 머신에서 4.3이 먼저 잡혀 애드온이 조용히 로드되지 않는 일이 없습니다. 최소 버전 미달만 있으면 찾은 설치와 버전을 함께 알립니다. `BLENDER_BINARY`를 지정하면 그 경로를 그대로 쓰고 낮은 버전은 경고만 합니다.
- Windows 실행기의 한국어 메시지가 파이프·리다이렉트에서 깨지던 문제를 고쳤습니다. UTF-8 BOM은 스크립트를 읽을 때만 쓰이고, 출력은 콘솔 코드페이지로 나가고 있었습니다. 이제 `$OutputEncoding`과 `[Console]::OutputEncoding`을 BOM 없는 UTF-8로 맞춥니다.
- 실행기 계약 정적 검사에 최소 버전 키와 탐색 함수 이름을 넣어, 자동 감지가 나중에 조용히 사라지지 않게 했습니다.
- `blender_retarget.py`에 간소화 경로 검사를 추가했습니다: 키가 절반 이하로 줄고, 모든 키가 오토 클램프 베지어이고, 부위별 4채널 키 위치가 일치하고, 모든 프레임 방향 오차가 3° 미만이며 발이 바닥 아래로 내려가지 않는지 확인합니다. 1~7번 기존 검사는 간소화를 끈 상태로 예전 보장을 그대로 지킵니다.

## 0.5.1 — 설치본 동봉 예제 탐색 수정

- 설치한 Extension에서 동봉 예제 모션이 목록에 나오지 않던 문제를 고쳤습니다. ZIP의 `motions/`는 패키지 디렉터리 안으로 풀리는데 코드가 한 단계 위 폴더를 보고 있었습니다. 이제 설치본과 개발 소스 배치를 모두 찾습니다.
- 배포 ZIP 검사가 모션 폴더를 직접 지정하던 부분을 없애고, 설치본이 동봉 예제를 스스로 찾는지 검사하도록 바꿨습니다. 이 결함은 폴더를 지정하던 예전 검사에 가려져 있었습니다.

## 0.5.0 — 공개 모션 485종과 리그 규격 흡수

- 공개 모션 카탈로그를 3종에서 **485종 / 35개 동작 분류**로 넓혔습니다. 걷기·달리기·점프·춤·발레·발차기·펀치·수영·농구·오르기·기기·살금살금·앉기·일어서기·계단·던지기·운반·균형·넘어짐·탈것 등을 한국어 분류 이름과 영어 원본 설명 태그로 검색합니다.
- `scripts/build_catalog.py`를 추가했습니다. CMU 공식 설명 인덱스와 고정 리비전 git 트리를 받아 분류별로 선별하고, HTTP Range로 각 BVH의 실제 프레임 수·FPS를 읽어 `catani/motion_catalog.json`을 생성합니다.
- 항목마다 정확한 바이트 크기와 **git blob SHA-1**을 담고, 다운로드 시 크기·해시·BVH 머리말을 모두 검증합니다. 체크섬이 없는 항목은 내려받지 않습니다.
- 본 이름 정규화를 도입했습니다. 접두사(`mixamorig:`, `DEF-`, `ORG-`, `Bip01`), 구분자, 대소문자, 장식 토큰(`fk`, `jnt`, `twist`)을 없애고 비교하므로 `upper_arm.L`, `upperarm_l`, `Bip01 L UpperArm`이 같은 부위로 인식됩니다. `ik`는 남겨 IK 컨트롤을 FK 본으로 잡지 않습니다.
- 지원 리그 규격을 3종에서 7종으로 늘렸습니다: CMU / cgspeed BVH, CMU ASF/AMC 원본 이름, Mixamo, Rigify / Player v1, Unreal / UE 스켈레톤, 3ds Max Biped, Daz Genesis.
- 규격으로 못 채운 부위는 별칭으로 찾고 **부모 부위의 자손인지 계층을 검증**한 뒤 받아들입니다. 규격 표에 없는 임의 이름 리그도 22개 부위를 채웁니다.
- **바닥 관통 보정**을 추가했습니다. 발이 바닥 아래로 내려가는 프레임에서 필요한 만큼만 몸 전체를 올리고, 보정량과 프레임 수를 리포트에 적습니다. 끌 수 있습니다.
- 검증이 키가 놓인 프레임만 재던 문제를 고쳤습니다. 구간 전체에 고르게 흩은 프레임으로 재므로 프레임 간격을 넓혔을 때의 보간 오차가 드러납니다.
- 손·머리가 바닥에 닿는 곡예 동작에서 남는 관통을 깊이와 본 이름으로 보고합니다.
- 다운로드 기본 위치를 설치된 애드온 폴더에서 **Blender 사용자 데이터 폴더**로 옮겼습니다. 읽기 전용 설치에서도 동작하고, 동봉 예제는 항상 함께 검색됩니다.
- 매핑 커버리지를 상태 줄과 리포트에 표시하고, 60% 미만이면 경고로 보고합니다.
- 실제 CMU 데이터 16개 동작 분류를 적용해 전부 22/22 부위·방향 오차 0.000°를 확인했습니다. 리그 규격 7종 회귀 검사를 추가했습니다.

## 0.4.0 — 검색 → 목록 → 적용

- 공개 카탈로그와 로컬 파일을 하나의 목록으로 합쳤습니다. 다운로드 목록과 내 모션 목록을 따로 보던 2단계 흐름을 없앴습니다.
- 주 패널을 검색 칸, 모션 목록, 대상 아마추어, 적용 버튼만 남기고 단순화했습니다. 검색은 입력하는 동안 즉시 목록을 좁히므로 갱신 버튼이 없습니다.
- 모션 폴더·프레임 간격·이동 적용·원본 숨기기와 출처·이용 조건·다운로드 주소·SHA-256은 별도 팝업(`ⓘ 출처`, `⚙ 상세`)으로 옮겼습니다.
- `적용` 한 번으로 필요한 공개 모션을 내려받고 캐릭터 아마추어에 리타게팅까지 수행합니다. 가져오기만 하던 이전 동작은 상세 팝업의 보조 기능으로 남겼습니다.
- 리타게팅은 CMU / cgspeed BVH, Mixamo, Rigify / Player v1 본 이름을 자동 판별해 최대 22개 부위를 잇고, 레스트 자세가 달라도 본 방향을 직접 맞춥니다. 엉덩이 이동은 첫 프레임 기준·다리 길이 비율로 맞춰 접지를 유지합니다.
- 적용 결과를 한 줄 상태(본 매핑 수, 프레임 범위, 키 수, 검증 방향 오차)와 상세 리포트(본 매핑 표, 규격, 이동 배율, 음소거한 NLA 트랙, 고정한 IK, 보존한 이전 Action)로 보고합니다.
- 결과를 덮어쓰던 활성 NLA 트랙과 IK 컨스트레인트를 굽기 전에 차단하고, 그 사실을 리포트에 남깁니다.
- 합성 CMU 규격 BVH로 22부위 리타게팅의 방향 오차·접지·NLA/IK 차단·재적용·실패 경로를 회귀 검사합니다.
- 배포 ZIP의 `motions.json`에서 개발 중 내려받은 모션 기록을 제외하고 동봉 예제만 남깁니다.

## 0.3.0 — 로컬 개발

- 로컬 `motions/` 폴더의 BVH/FBX 모션 샘플을 검색하고 Blender로 가져오는 모션 라이브러리 기반을 추가했습니다.
- CMU 기반 BVH 3개를 내려받는 공개 모션 카탈로그와 SHA-256 검증 다운로드를 추가했습니다.
- `motions.json` 메타데이터로 모션 이름·태그·설명을 관리하고, 검색 결과에서 선택한 모션을 별도 컬렉션에 가져옵니다.
- 가져온 모션은 현재 리그에 바로 덮어쓰지 않고 리타게팅 기준 원본으로 유지합니다.
- 자연어 기반 동작 명세 생성 UI와 관련 런타임 모듈을 제거했습니다.
- 모션 라이브러리 인덱스, 다운로드, 실제 Blender BVH 가져오기 UI 경로를 회귀 검사합니다.

## 0.2.x — 폐기한 로컬 실험

- 제한된 절차 동작 생성 실험을 진행했으나, 품질과 확장성 문제로 0.3.0에서 모션 라이브러리 기반으로 전환했습니다.

## 0.1.1 — 로컬 개발

- 매 프레임 키를 주요 포즈와 Bézier 곡선으로 대체했습니다.

## 0.1.0 — 로컬 개발

- 샘플 리그의 대기·손 흔들기, 복제 미리보기·확정·취소를 구현했습니다.
